{% extends "base.html.twig" %}

{% block title %}{{ title }}{% endblock %}
{% block content %}

    <div class="starter-template">
        <h1>{{ title }}</h1>
        <p class="lead">{{ content }}</p>
    </div>

{% endblock %}
